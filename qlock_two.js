// JavaScript Document

// Variables 
// 'IT' variable
var iInIt = document.querySelector(".iInIt");
var tInIt = document.querySelector(".tInIt");

var itClass = [iInIt, tInIt]; 

// 'IS' variable
var iInIs = document.querySelector(".iInIs");
var sInIs = document.querySelector(".sInIs");

var isClass = [iInIs, sInIs];

// 'A' variable
var aInA = document.querySelector(".aInA");

var aClass = [aInA]; 

// 'QUARTER' variable
var qInQuarter = document.querySelector(".qInQuarter");
var uInQuarter = document.querySelector(".uInQuarter");
var aInQuarter = document.querySelector(".aInQuarter");
var rInQuarter = document.querySelector(".rInQuarter");
var tInQuarter = document.querySelector(".tInQuarter");
var eInQuarter = document.querySelector(".eInQuarter");
var r2InQuarter = document.querySelector(".r2InQuarter");

var quarterClass = [qInQuarter, uInQuarter, aInQuarter, rInQuarter, tInQuarter, eInQuarter, r2InQuarter];

// 'TWENTY' variable
var tInTwenty = document.querySelector(".tInTwenty");
var wInTwenty = document.querySelector(".wInTwenty");
var eInTwenty = document.querySelector(".eInTwenty");
var nInTwenty = document.querySelector(".nInTwenty");
var t2InTwenty = document.querySelector(".t2InTwenty");
var yInTwenty = document.querySelector(".yInTwenty");

var twentyClass = [tInTwenty, wInTwenty, eInTwenty, nInTwenty, t2InTwenty, yInTwenty];

// 'FIVE 1' variable
var fInFive = document.querySelector(".fInFive");
var iInFive = document.querySelector(".iInFive");
var vInFive = document.querySelector(".vInFive");
var eInFive = document.querySelector(".eInFive");

var fiveOneClass = [fInFive, iInFive, vInFive, eInFive];

// 'HALF' variable
var hInHalf = document.querySelector(".hInHalf");
var aInHalf = document.querySelector(".aInHalf");
var lInHalf = document.querySelector(".lInHalf");
var fInHalf = document.querySelector(".fInHalf");

var halfClass = [hInHalf, aInHalf, lInHalf, fInHalf];

// 'PAST' variable
var pInPast = document.querySelector(".pInPast");
var aInPast = document.querySelector(".aInPast");
var sInPast = document.querySelector(".sInPast");
var tInPast = document.querySelector(".tInPast");

var pastClass = [pInPast, aInPast, sInPast, tInPast];

// 'TEN' variable
var tInTen = document.querySelector(".tInTen");
var eInTen = document.querySelector(".eInTen");
var nInTen = document.querySelector(".nInTen");

var tenClass = [tInTen, eInTen, nInTen];

// 'TO' variable
var tInTo = document.querySelector(".tInTo");
var oInTo = document.querySelector(".oInTo");

var toClass = [tInTo, oInTo];

// 'OCLOCK' variable
var oInOclock = document.querySelector(".oInOclock");
var cInOclock = document.querySelector(".cInOclock");
var lInOclock = document.querySelector(".lInOclock");
var oclOckInOclock = document.querySelector(".oclOckInFOclock");
var cloCkInOclock = document.querySelector(".cloCkInOclock");
var kInOclock = document.querySelector(".kInFOclock");

var oclockClass = [oInOclock, cInOclock, lInOclock, oclOckInOclock, cloCkInOclock, kInOclock]; 

// Variables by Hour
// One Oclock
var oInOneOclock = document.querySelector(".oInOneOclock");
var nInOneOclock = document.querySelector(".nInOneOclock");
var eInOneOclock = document.querySelector(".eInOneOclock");

var oneOclock = [oInOneOclock, nInOneOclock, eInOneOclock];

// TWO OCLOCK
var tInTwoOclock = document.querySelector(".tInTwoOclock");
var wInTwoOclock = document.querySelector(".wInTwoOclock");
var oInTwoOclock = document.querySelector(".oInTwoOclock");

var twoOclock = [tInTwoOclock, wInTwoOclock, oInTwoOclock];

// THREE OCLOCK
var tInThreeOclock = document.querySelector(".tInThreeOclock");
var hInThreeOclock = document.querySelector(".hInThreeOclock");
var rInThreeOclock = document.querySelector(".rInThreeOclock");
var eInThreeOclock = document.querySelector(".eInThreeOclock");
var e2InThreeOclock = document.querySelector(".e2InThreeOclock");

var threeOclock = [tInThreeOclock, hInThreeOclock, rInThreeOclock, eInThreeOclock, e2InThreeOclock];

// FOUR OCLOCK
var fInFourOclock = document.querySelector(".fInFourOclock");
var oInFourOclock = document.querySelector(".oInFourOclock");
var uInFourOclock = document.querySelector(".uInFourOclock");
var rInFourOclock = document.querySelector(".rInFourOclock");

var fourOclock = [fInFourOclock, oInFourOclock, uInFourOclock, rInFourOclock];

// FIVE OCLOCK
var fInFiveOclock = document.querySelector(".fInFiveOclock");
var iInFiveOclock = document.querySelector(".iInFiveOclock");
var vInFiveOclock = document.querySelector(".vInFiveOclock");
var eInFiveOclock = document.querySelector(".eInFiveOclock");

var fiveOclock = [fInFiveOclock, iInFiveOclock, vInFiveOclock, eInFiveOclock];

// SIX OCLOCK
var sInSixOclock = document.querySelector(".sInSixOclock");
var iInSixOclock = document.querySelector(".iInSixOclock");
var xInSixOclock = document.querySelector(".xInSixOclock");

var sixOclock = [sInSixOclock, iInSixOclock, xInSixOclock];

// SEVEN OCLOCK
var sInSevenOclock = document.querySelector(".sInSevenOclock");
var eInSevenOclock = document.querySelector(".eInSevenOclock");
var vInSevenOclock = document.querySelector(".vInSevenOclock");
var e2InSevenOclock = document.querySelector(".e2InSevenOclock");
var nInSevenOclock = document.querySelector(".nInSevenOclock");

var sevenOclock = [sInSevenOclock, eInSevenOclock, vInSevenOclock, e2InSevenOclock, nInSevenOclock];

// EIGHT OCLOCK
var eInEightOclock = document.querySelector(".eInEightOclock");
var iInEightOclock = document.querySelector(".iInEightOclock");
var gInEightOclock = document.querySelector(".gInEightOclock");
var hInEightOclock = document.querySelector(".hInEightOclock");
var tInEightOclock = document.querySelector(".tInEightOclock");

var eightOclock = [eInEightOclock, iInEightOclock, gInEightOclock, hInEightOclock, tInEightOclock];

// NINE OCLOCK
var nInNineOclock = document.querySelector(".nInNineOclock");
var iInNineOclock = document.querySelector(".iInNineOclock");
var n2InNineOclock = document.querySelector(".n2InNineOclock");
var eInNineOclock = document.querySelector(".eInNineOclock");

var nineOclock = [nInNineOclock, iInNineOclock, n2InNineOclock, eInNineOclock];

// TEN OCLOCK
var tInTenOclock = document.querySelector(".tInTenOclock");
var eInTenOclock = document.querySelector(".eInTenOclock");
var nInTenOclock = document.querySelector(".nInTenOclock");

var tenOclock = [tInTenOclock, eInTenOclock, nInTenOclock];

// ELEVEN OCLOCK
var eInElevenOclock = document.querySelector(".eInElevenOclock");
var lInElevenOclock = document.querySelector(".lInElevenOclock");
var e2InEleven2Oclock = document.querySelector(".e2InEleven2Oclock");
var vInElevenOclock = document.querySelector(".vInElevenOclock");
var e3InEleven3Oclock = document.querySelector(".e3InEleven3Oclock");
var nInElevenOclock = document.querySelector(".nInElevenOclock");

var elevenOclock = [eInElevenOclock, lInElevenOclock, e2InEleven2Oclock, vInElevenOclock, e3InEleven3Oclock, nInElevenOclock ];

// TWELVE OCLOCK
var tInTwelveOclock = document.querySelector(".tInTwelveOclock");
var wInTwelveOclock = document.querySelector(".wInTwelveOclock");
var eInTwelveOclock = document.querySelector(".eInTwelveOclock");
var lInTwelveOclock = document.querySelector(".lInTwelveOclock");
var vInTwelveOclock = document.querySelector(".vInTwelveOclock");
var e2InTwelveOclock = document.querySelector(".e2InTwelveOclock");

var twelveOclock = [tInTwelveOclock, wInTwelveOclock, eInTwelveOclock, lInTwelveOclock, vInTwelveOclock, e2InTwelveOclock];

// MINUTE BUBBLES
var oneMin = document.querySelector(".oneMin");

var oneMinList = [oneMin];
                    
var twoMin = document.querySelector(".twoMin");

var twoMinList = [twoMin];

var threeMin = document.querySelector(".threeMin");

var threeMinList = [threeMin];

var fourMin = document.querySelector(".fourMin");

var fourMinList = [fourMin];
    

// SCRIPT REFRESH
window.setInterval(function(){

    // GET TIME VARIABLES
    var d = new Date();
    var minute = d.getMinutes();
    var hour = d.getHours();

    // IT IS {HOUR} OCLOCK
    if(minute >= 0 && minute <= 4) {
        
        itClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        isClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        fiveOneClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        toClass.forEach(function(el) {
            el.classList.remove("lit");
        });
        

        itClass.forEach(function(el) {
            el.classList.add("lit");
        });

        isClass.forEach(function(el) {
            el.classList.add("lit");
        });

        oclockClass.forEach(function(el) {
            el.classList.add("lit");
        });	
    }

    // IT IS FIVE PAST {HOUR}
    if(minute >= 5 && minute <= 9) {
        
        itClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        isClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        oclockClass.forEach(function(el) {
            el.classList.remove("lit");
        });	
        
        
        itClass.forEach(function(el) {
            el.classList.add("lit");
        });

        isClass.forEach(function(el) {
            el.classList.add("lit");
        });

        fiveOneClass.forEach(function(el) {
            el.classList.add("lit");
        });

        pastClass.forEach(function(el) {
            el.classList.add("lit");
        });
    }

    // IT IS TEN PAST {HOUR}
    if(minute >= 10 && minute <= 14) {

        itClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        isClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        fiveOneClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        pastClass.forEach(function(el) {
            el.classList.remove("lit");
        });
        
        
        itClass.forEach(function(el) {
            el.classList.add("lit");
        });

        isClass.forEach(function(el) {
            el.classList.add("lit");
        });

        tenClass.forEach(function(el) {
            el.classList.add("lit");
        });

        pastClass.forEach(function(el) {
            el.classList.add("lit");
        });
    }

    // IT IS A QUARTER PAST {HOUR}
    if(minute >= 15 && minute <= 19) {

        itClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        isClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        tenClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        pastClass.forEach(function(el) {
            el.classList.remove("lit");
        });
        
        
        itClass.forEach(function(el) {
            el.classList.add("lit");
        });

        isClass.forEach(function(el) {
            el.classList.add("lit");
        });

        aClass.forEach(function(el) {
            el.classList.add("lit");
        });

        pastClass.forEach(function(el) {
            el.classList.add("lit");
        });

        quarterClass.forEach(function(el) {
            el.classList.add("lit");
        });
    }

    // IT IS TWENTY PAST {HOUR}
    if(minute >= 20 && minute <= 24) {

        itClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        isClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        aClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        pastClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        quarterClass.forEach(function(el) {
            el.classList.remove("lit");
        });
        
        
        itClass.forEach(function(el) {
            el.classList.add("lit");
        });

        isClass.forEach(function(el) {
            el.classList.add("lit");
        });

        twentyClass.forEach(function(el) {
            el.classList.add("lit");
        });

        pastClass.forEach(function(el) {
            el.classList.add("lit");
        });
    }

    // IT IS TWENTY FIVE PAST {HOUR}
    if(minute >= 25 && minute <= 29) {
        
        itClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        isClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        twentyClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        pastClass.forEach(function(el) {
            el.classList.remove("lit");
        });
        

        itClass.forEach(function(el) {
            el.classList.add("lit");
        });

        isClass.forEach(function(el) {
            el.classList.add("lit");
        });

        twentyClass.forEach(function(el) {
            el.classList.add("lit");
        });

        fiveOneClass.forEach(function(el) {
            el.classList.add("lit");
        });

        pastClass.forEach(function(el) {
            el.classList.add("lit");
        });
    }

    // IT IS HALF PAST {HOUR}
    if(minute >= 30 && minute <= 34) {
        
        itClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        isClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        twentyClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        fiveOneClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        pastClass.forEach(function(el) {
            el.classList.remove("lit");
        });
        

        itClass.forEach(function(el) {
            el.classList.add("lit");
        });

        isClass.forEach(function(el) {
            el.classList.add("lit");
        });

        pastClass.forEach(function(el) {
            el.classList.add("lit");
        });

        halfClass.forEach(function(el) {
            el.classList.add("lit");
        });
    }

    // IT IS TWENTY FIVE TO {NEXT-HOUR}
    if(minute >= 35 && minute <= 39) {
        
        itClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        isClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        pastClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        halfClass.forEach(function(el) {
            el.classList.remove("lit");
        });
        

        itClass.forEach(function(el) {
            el.classList.add("lit");
        });

        isClass.forEach(function(el) {
            el.classList.add("lit");
        });

        twentyClass.forEach(function(el) {
            el.classList.add("lit");
        });

        fiveOneClass.forEach(function(el) {
            el.classList.add("lit");
        });

        toClass.forEach(function(el) {
            el.classList.add("lit");
        });
    }

    // IT IS TWENTY TO {NEXT-HOUR}
    if(minute >= 40 && minute <= 44) {
        
        itClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        isClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        twentyClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        fiveOneClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        toClass.forEach(function(el) {
            el.classList.remove("lit");
        });
        

        itClass.forEach(function(el) {
            el.classList.add("lit");
        });

        isClass.forEach(function(el) {
            el.classList.add("lit");
        });

        twentyClass.forEach(function(el) {
            el.classList.add("lit");
        });

        toClass.forEach(function(el) {
            el.classList.add("lit");
        });
    }


    // IT IS A QUARTER TO {NEXT-HOUR}
    if(minute >= 45 && minute <= 49) {
        
        itClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        isClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        twentyClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        toClass.forEach(function(el) {
            el.classList.remove("lit");
        });
        

        itClass.forEach(function(el) {
            el.classList.add("lit");
        });

        isClass.forEach(function(el) {
            el.classList.add("lit");
        });

        aClass.forEach(function(el) {
            el.classList.add("lit");
        });

        quarterClass.forEach(function(el) {
            el.classList.add("lit");
        });

        toClass.forEach(function(el) {
            el.classList.add("lit");
        });
    }

    //  IT IS TEN TO {NEXT-HOUR}
    if(minute >= 50 && minute <= 54) {
        
        itClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        isClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        aClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        quarterClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        toClass.forEach(function(el) {
            el.classList.remove("lit");
        });
        

        itClass.forEach(function(el) {
            el.classList.add("lit");
        });

        isClass.forEach(function(el) {
            el.classList.add("lit");
        });

        tenClass.forEach(function(el) {
            el.classList.add("lit");
        });

        toClass.forEach(function(el) {
            el.classList.add("lit");
        });
    }

    // IT IS FIVE TO {NEXT-HOUR}
    if(minute >= 55 && minute <= 59) {
        
        itClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        isClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        tenClass.forEach(function(el) {
            el.classList.remove("lit");
        });

        toClass.forEach(function(el) {
            el.classList.remove("lit");
        });
        

        itClass.forEach(function(el) {
            el.classList.add("lit");
        });

        isClass.forEach(function(el) {
            el.classList.add("lit");
        });

        fiveOneClass.forEach(function(el) {
            el.classList.add("lit");
        });

        toClass.forEach(function(el) {
            el.classList.add("lit");
        });
    }

    // 1 OCLOCK AM
    if(hour == 1) {
        if(minute >= 0 && minute <= 34) {
			oneOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            oneOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			oneOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            twoOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
    }

    // 2 OCLOCK AM                     
    if(hour == 2) {
        if(minute >= 0 && minute <= 34) {
			twoOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            twoOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			twoOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            threeOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
    }

    // 3 OCLOCK AM
    if(hour == 3) {
        if(minute >= 0 && minute <= 34) {
			threeOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            threeOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			threeOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            fourOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
    }  

    // 4 OCLOCK AM
    if(hour == 4) {
        if(minute >= 0 && minute <= 34) {
			fourOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            fourOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			fourOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            fiveOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
    }

    // 5 OCLOCK AM
    if(hour == 5) {
        if(minute >= 0 && minute <= 34) {
			fiveOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            fiveOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			fiveOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            sixOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
    }

    // 6 OCLOCK AM
    if(hour == 6) {
        if(minute >= 0 && minute <= 34) {
			sixOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            sixOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			sixOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            sevenOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        } 
    }

    // 7 OCLOCK AM
    if(hour == 7) {
        if(minute >= 0 && minute <= 34) {
			sevenOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            sevenOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			sevenOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            eightOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        } 
    }

    // 8 OCLOCK AM
    if(hour == 8) {
        if(minute >= 0 && minute <= 34) {
			eightOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            eightOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			eightOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            nineOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        } 
    }

    // 9 OCLOCK AM
    if(hour == 9) {
        if(minute >= 0 && minute <= 34) {
			nineOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            nineOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			nineOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            tenOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        } 
    }

    // 10 OCLOCK AM
    if(hour == 10) {
        if(minute >= 0 && minute <= 34) {
			tenOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            tenOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			tenOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            elevenOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        } 
    }

    // 11 OCLOCK AM
    if(hour == 11) {
        if(minute >= 0 && minute <= 34) {
			elevenOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            elevenOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			elevenOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            twelveOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        } 
    }

    // 12 OCLOCK AM
    if(hour == 12) {
        if(minute >= 0 && minute <= 34) {
			twelveOclock.forEach(function(el) {
                el.classList.add("lit");
            });
            twelveOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			twelveOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            oneOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        } 
    }  
    
    // 1 OCLOCK PM
    if(hour == 13) {
        if(minute >= 0 && minute <= 34) {
			oneOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            oneOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			oneOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            twoOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
    }

    // 2 OCLOCK PM                     
    if(hour == 14) {
        if(minute >= 0 && minute <= 34) {
			twoOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            twoOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			twoOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            threeOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
    }

    // 3 OCLOCK PM
    if(hour == 15) {
        if(minute >= 0 && minute <= 34) {
			threeOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            threeOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			threeOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            fourOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
    }  

    // 4 OCLOCK PM
    if(hour == 16) {
        if(minute >= 0 && minute <= 34) {
			fourOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            fourOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			fourOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            fiveOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
    }

    // 5 OCLOCK PM
    if(hour == 17) {
        if(minute >= 0 && minute <= 34) {
			fiveOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            fiveOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			fiveOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            sixOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
    }

    // 6 OCLOCK PM
    if(hour == 18) {
        if(minute >= 0 && minute <= 34) {
			sixOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            sixOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			sixOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            sevenOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        } 
    }

    // 7 OCLOCK PM
    if(hour == 19) {
        if(minute >= 0 && minute <= 34) {
			sevenOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            sevenOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			sevenOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            eightOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        } 
    }

    // 8 OCLOCK PM
    if(hour == 20) {
        if(minute >= 0 && minute <= 34) {
			eightOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            eightOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			eightOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            nineOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        } 
    }

    // 9 OCLOCK PM
    if(hour == 21) {
        if(minute >= 0 && minute <= 34) {
			nineOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            nineOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			nineOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            tenOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        } 
    }

    // 10 OCLOCK PM
    if(hour == 22) {
        if(minute >= 0 && minute <= 34) {
			tenOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            tenOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			tenOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            elevenOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        } 
    }

    // 11 OCLOCK PM
    if(hour == 23) {
        if(minute >= 0 && minute <= 34) {
			elevenOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            elevenOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			elevenOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            twelveOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        } 
    }

    // 12 OCLOCK PM
    if(hour == 24) {
        if(minute >= 0 && minute <= 34) {
			twelveOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            twelveOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        }
        if(minute >= 35 && minute <= 59) {
			twelveOclock.forEach(function(el) {
                el.classList.remove("lit");
            });
            oneOclock.forEach(function(el) {
                el.classList.add("lit");
            });
        } 
    }  
    
    if (minute == 1 || minute == 6 || minute == 11 || minute == 16 || minute == 21 || minute == 26 || minute == 31 || minute == 36 || minute == 41 || minute == 46 || minute == 51 || minute == 56) {
            oneMinList.forEach(function(el) {
                el.classList.add("litbubble");
            });
    }
    
    if (minute == 2 || minute == 7 || minute == 12 || minute == 17 || minute == 22 || minute == 27 || minute == 32 || minute == 37 || minute == 42 || minute == 47 || minute == 52 || minute == 57) {
            
            oneMinList.forEach(function(el) {
                el.classList.add("litbubble");
            });
        
            twoMinList.forEach(function(el) {
                el.classList.add("litbubble");
            });
    }
        
    if (minute == 3 || minute == 8 || minute == 13 || minute == 18 || minute == 23 || minute == 28 || minute == 33 || minute == 38 || minute == 43 || minute == 48 || minute == 53 || minute == 58) {
            
            oneMinList.forEach(function(el) {
                el.classList.add("litbubble");
            });
        
            twoMinList.forEach(function(el) {
                el.classList.add("litbubble");
            });
        
            threeMinList.forEach(function(el) {
                el.classList.add("litbubble");
            });
    }
        
    if (minute == 4 || minute == 9 || minute == 14 || minute == 19 || minute == 24 || minute == 29 || minute == 34 || minute == 39 || minute == 44 || minute == 49 || minute == 54 || minute == 59) {
            
            oneMinList.forEach(function(el) {
                el.classList.add("litbubble");
            });
        
            twoMinList.forEach(function(el) {
                el.classList.add("litbubble");
            });
        
            threeMinList.forEach(function(el) {
                el.classList.add("litbubble");
            });
        
            fourMinList.forEach(function(el) {
                el.classList.add("litbubble");
            });
    }
    
    if (minute == 0 || minute == 5 || minute == 10 || minute == 15 || minute == 20 || minute == 25 || minute == 30 || minute == 35 || minute == 60 || minute == 45 || minute == 50 || minute == 55 || minute == 60) {
            
            oneMinList.forEach(function(el) {
                el.classList.remove("litbubble");
            });
        
            twoMinList.forEach(function(el) {
                el.classList.remove("litbubble");
            });
        
            threeMinList.forEach(function(el) {
                el.classList.remove("litbubble");
            });
        
            fourMinList.forEach(function(el) {
                el.classList.remove("litbubble");
            });
    }
	
}, 1000);

